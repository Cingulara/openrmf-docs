# Add Score indexes
docker exec mongodb-score mongo --username root --password "$1" --eval "db = db.getSiblingDB('openrmfscore'); db.Scores.createIndex({ created: -1 }); db.Scores.createIndex({ updatedOn: -1 }); db.Scores.createIndex({ totalCat1Open: 1 }); db.Scores.createIndex({ totalCat1NotApplicable: 1 }); db.Scores.createIndex({ totalCat1NotAFinding: 1 }); db.Scores.createIndex({ totalCat1NotReviewed: 1 }); db.Scores.createIndex({ totalCat2Open: 1 }); db.Scores.createIndex({ totalCat2NotApplicable: 1 }); db.Scores.createIndex({ totalCat2NotAFinding: 1 }); db.Scores.createIndex({ totalCat2NotReviewed: 1 }); db.Scores.createIndex({ totalCat3Open: 1 }); db.Scores.createIndex({ totalCat3NotApplicable: 1 }); db.Scores.createIndex({ totalCat3NotAFinding: 1 }); db.Scores.createIndex({ totalCat3NotReviewed: 1 });"