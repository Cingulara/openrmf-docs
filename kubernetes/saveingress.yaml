apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: openrmf-save-ingress
  namespace: openrmf
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: save.openrmf
    http:
      paths:
      - path: /
        backend:
          serviceName: openrmf-save
          servicePort: 8080